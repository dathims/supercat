'use strict';

/**
 * @ngdoc function
 * @name supercatApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the supercatApp
 */
angular.module('supercatApp')
  .controller('LoginCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
